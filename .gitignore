# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# Flutter/Dart/Pub
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
# NOTE: pubspec.lock is committed on purpose (app, not package) for reproducible CI builds.

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android
android/.gradle/
android/captures/
android/local.properties
android/**/GeneratedPluginRegistrant.java
android/key.properties
**/*.keystore
**/*.jks

# iOS
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/Flutter/Generated.xcconfig
ios/Flutter/ephemeral/
ios/Flutter/app.flx
ios/Flutter/flutter_export_environment.sh
ios/Pods/
ios/.symlinks/
ios/ServiceDefinitions.json
ios/Runner/GeneratedPluginRegistrant.*
ios/**/*.mobileprovision
ios/**/*.p12
**/AuthKey_*.p8

# macOS / Linux / Windows desktop ephemerals
**/Flutter/ephemeral/
linux/flutter/ephemeral/
macos/Flutter/ephemeral/
windows/flutter/ephemeral/

# Cleanup staging
_to_delete/

# Secrets
*.env

# TTS A/B probe output (regenerate with seed_heteronym_audio.ps1)
vo_heteronyms/

# Pronunciation pipeline working files (regenerate with tools/build_pronunciation_dict.py)
tools/pronunciation/*
# Diagnostic probe output (regenerate with probe_tts.ps1)
vo_probe/

# Golden captures from the opt-in frame tests. Rasterized by the host font
# engine, so they only match the machine that made them - regenerate with
#   QULEX_CAPTURE=1 flutter test --update-goldens
test/frames/

# Test and tool scratch state — regenerated, never reviewed.
test/failures/
tools/.example_batches.json
tools/.warm_audio_state.json

tools/.warm_audio_state*.json

!tools/pronunciation/verdict_ledger.json
